import 'package:flutter/material.dart';

class FragmentNotfication extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Fragment Notification'));
  }
}
